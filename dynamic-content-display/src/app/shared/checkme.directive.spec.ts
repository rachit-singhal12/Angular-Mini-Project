import { CheckmeDirective } from './checkme.directive';

describe('CheckmeDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckmeDirective();
    expect(directive).toBeTruthy();
  });
});
